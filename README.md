# DistanceBetweenPointsCalculator
training in OOP C#:
* Inheritance (the base class and the derived class have own constructors)
* access modifiers (public, private, protected), readonly
* static classes and methods
